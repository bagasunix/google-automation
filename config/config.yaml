# Search Automation Config
# Edit domain list, engine ratio, dan settings sesuai kebutuhan

domains:
  - bagasunix.com
  # - example.com    # bisa tambah/ubah kapan saja

engine_ratio:
  google: 70
  bing: 30

scheduler:
  max_search_per_proxy: 1          # 1 proxy = 1 search per cycle (STRICT)
  new_article_boost: 5             # first week: 5 searches
  regular_max: 3                   # after boost: 3 searches max
  captcha_pause_hours: 3           # auto pause when CAPTCHA spike
  min_cooldown_seconds: 30         # between tasks
  max_cooldown_seconds: 120
  post_exit_cooldown_min: 30       # cooldown after exiting article (seconds)
  post_exit_cooldown_max: 120
  active_hours_start: 7            # 7am local proxy time
  active_hours_end: 23             # 11pm local proxy time

proxy:
  refresh_interval_hours: 3        # scrape new proxies every N hours
  health_check_timeout: 8          # seconds per proxy
  webshare_api_key: "4o2y8qp2pijurc2gefxjnsyff53ljmhqnsa6zjg6"  # Webshare API token
  sources:
    # GitHub raw lists (auto-updated, most reliable)
    - https://raw.githubusercontent.com/TheSpeedX/PROXY-List/master/http.txt
    - https://raw.githubusercontent.com/monosans/proxy-list/main/proxies/http.txt
    - https://raw.githubusercontent.com/clarketm/proxy-list/master/proxy-list-raw.txt
    - https://raw.githubusercontent.com/jetkai/proxy-list/main/online-proxies/txt/proxies-http.txt
    - https://raw.githubusercontent.com/roosterkid/openproxylist/main/HTTPS_RAW.txt
    # API endpoints
    - https://api.proxyscrape.com/v4/free-proxy-list/get?request=display_proxies&proxy_format=protocolipport&format=text&protocol=http
    - https://proxylist.geonode.com/api/proxy-list?limit=500&page=1&sort_by=lastChecked&sort_type=desc
    # HTML pages
    - https://free-proxy-list.net/
    - https://spys.one/en/http-proxy-list/

grpc:
  port: 50051
  worker_timeout: 300              # seconds per task (reading simulation takes time)

article_collection:
  method: sitemap                  # sitemap | rss | html | manual
  refresh_interval_hours: 6        # re-scrape domain for new articles
